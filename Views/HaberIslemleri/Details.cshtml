@model TeknolojiHaber.Models.Haber

@{
    ViewData["Title"] = "Haberin Detayları";
}

<br><br>
<h1>Haberin Detayları</h1>

<div>
    <hr />
    <div class="card-columns row m-t-md ">@foreach (var item in Model.Resimler) 
 { 
  <div class="card col-xs-2 col-md-2">
    <img class="img-fluid img-zoom" src="\resimler\@item.DosyaAdi"  style="height: 150px; widht: 100px; margin-top: 25px; center " alt="" />
    <div class="card-body">
    </div>
  </div>
  }
    @* <dl class="row">
        <div class="col-7">
            <h4 class="text-danger">Resim</h4>
            <hr />
            @foreach (var item in Model.HaberResimler)
            {
                <img src="\resimler\@item.DosyaAdi" style="max-height: 100px;" class="rounded"/>
            }
        </div> *@
        <div class="col-10">
            <hr />
        <dt class = "col-sm-2">
            Haberin Başlığı
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.HaberBaslik)
        </dd>
        <dt class = "col-sm-2">
            Haberin Bilgisi
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.HaberBilgi)
        </dd>
        <dt class = "col-sm-2">
            Haberin Tarihi
        </dt>
        <dd class = "col-sm-10">
        @Html.DisplayFor(model => model.HaberTarih)
        </dd>
        <dt class = "col-sm-10">
            Haberin İlgi Düzeyi
        </dt>
        <dd class = "col-sm-10">
        @Html.DisplayFor(model => model.IlgiDuzeyi)
        </dd>
        </div>
        
    </dl>

</div><hr />
<div>
    @if (User.IsInRole("Admin")||User.IsInRole("Editor"))
    {
      <a class="fas fa-info-circle d-inline p-2 bg-info text-white" asp-action="Edit" asp-route-id="@Model.Id"> Düzenle</a>  
    }
    <a class="d-inline p-2 bg-primary text-white" asp-action="Index">Listeye Geri Dön</a>
</div>
@model IEnumerable<TeknolojiHaber.Models.Haber>
@{
    ViewData["Title"] = "Anasayfa";
}

<h1 style="text-align:center">Güncel Teknoloji Haberleri</h1>
<br>


<div class="slideshow-container">
@foreach (var item in Model)
    {
  <!-- Full-width images with number and caption text -->
  <div class="mySlides">
    <img class="img-fluid" src="\resimler\@item.Resimler.FirstOrDefault().DosyaAdi"  style="width:100%" />
    <div class="text">@item.HaberBaslik</div>
  </div>
}

  <!-- Next and previous buttons -->
  <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
  <a class="next" onclick="plusSlides(1)">&#10095;</a>
</div>
<br>




<script type="text/javascript">
var slideIndex = 1;
showSlides(slideIndex);

// Next/previous controls
function plusSlides(n) {
  showSlides(slideIndex += n);
}

// Thumbnail image controls
function currentSlide(n) {
  showSlides(slideIndex = n);
}

function showSlides(n) {
  var i;
  var slides = document.getElementsByClassName("mySlides");
  if (n > slides.length) {slideIndex = 1}
  if (n < 1) {slideIndex = slides.length}
  for (i = 0; i < slides.length; i++) {
      slides[i].style.display = "none";
  }
  slides[slideIndex-1].style.display = "block";
  dots[slideIndex-1].className += " active";
}
</script>